% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pointblank_setup.R
\name{setup_pointblank_agent}
\alias{setup_pointblank_agent}
\title{Setup pointblank agent for data validation}
\usage{
setup_pointblank_agent(
  cmp,
  cols_reference,
  common_cols,
  tol_cols,
  row_validation_info = NULL,
  ref_suffix,
  warn_at,
  stop_at,
  label,
  na_equal,
  lang = getOption("datadiff.lang", "en"),
  locale = getOption("datadiff.locale", "en_US"),
  missing_in_candidate = character(0),
  type_mismatch_cols = character(0),
  add_col_exists_steps = TRUE
)
}
\arguments{
\item{cmp}{Comparison dataframe with candidate and reference data}

\item{cols_reference}{Character vector of reference column names}

\item{common_cols}{Character vector of columns present in both datasets}

\item{tol_cols}{Character vector of columns with tolerance rules}

\item{row_validation_info}{List with row validation information from validate_row_counts}

\item{ref_suffix}{Suffix for reference columns}

\item{warn_at}{Warning threshold (fraction of failing tests)}

\item{stop_at}{Stop threshold (fraction of failing tests)}

\item{label}{Descriptive label for the validation}

\item{na_equal}{Logical indicating if NA values are considered equal}

\item{lang}{Language code for pointblank reports. Defaults to the
\code{datadiff.lang} option if set, otherwise \code{"en"}. Set globally
with \code{options(datadiff.lang = "fr")}. Supported values include
"en" (English), "fr" (French), "de" (German), "it" (Italian), "es" (Spanish),
"pt" (Portuguese), "zh" (Chinese), "ja" (Japanese), "ru" (Russian), etc.
See pointblank documentation for full list.}

\item{locale}{Locale code for number and date formatting. Defaults to the
\code{datadiff.locale} option if set, otherwise \code{"en_US"}. Set globally
with \code{options(datadiff.locale = "fr_FR")}. Examples: "en_GB", "fr_FR",
"de_DE", "es_ES", "pt_BR", "zh_CN", "ja_JP".}

\item{missing_in_candidate}{Character vector of columns missing in candidate dataset}

\item{type_mismatch_cols}{Character vector of columns whose type differs between
reference and candidate (e.g. numeric in reference, character in candidate).
A dedicated failing validation step labelled \verb{type_mismatch: <column>} is
added for each such column.}

\item{add_col_exists_steps}{Logical indicating whether to add \code{col_exists} validation
steps for common columns (default: \code{TRUE}). Set to \code{FALSE} for the non-local (lazy
table) path where \code{cmp} only contains pre-computed boolean columns, not the original
data columns.}
}
\value{
Configured pointblank agent ready for interrogation
}
\description{
Creates and configures a pointblank validation agent with all necessary validation steps
including column existence checks, exact value comparisons, and tolerance validations.
}
\examples{
cmp <- data.frame(a = 1:3, a__reference = 1:3, b = c(1.1, 2.2, 3.3),
 b__reference = c(1.0, 2.0, 3.0))
row_info <- list(check_count = FALSE)
setup_pointblank_agent(cmp, c("a", "b"), c("a", "b"), "b", row_info,
 "__reference", 0.1, 0.1, "Test", TRUE)
}
