% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_types.R
\name{derive_column_rules}
\alias{derive_column_rules}
\title{Derive column-specific validation rules}
\usage{
derive_column_rules(data_reference, rules)
}
\arguments{
\item{data_reference}{Reference dataframe used to determine column types}

\item{rules}{List of validation rules from read_rules()}
}
\value{
List of column-specific rules with type and name-based rules merged
}
\description{
Combines type-based and column-specific validation rules into a unified
list of rules for each column in the dataset.
}
\examples{
df <- data.frame(a = 1:3, b = c(1.1, 2.2, 3.3))
rules <- list(by_type = list(numeric = list(abs = 0.1)), by_name = list(a = list(abs = 0.5)))
derive_column_rules(df, rules)
}
